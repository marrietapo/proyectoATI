import { useContext } from "react";
import { BarbershopContext } from "../providers/BarbershopProvider";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(BarbershopContext);
